# Copy to `.env`. Defaults are baked in, so the app runs without this file.

SECRET_KEY=dev-secret-change-me
ACCESS_TOKEN_MINUTES=720
DATABASE_PATH=organ_network.db

# When the DB is first created, seed demo hospitals + an admin account.
SEED_DEMO=1
